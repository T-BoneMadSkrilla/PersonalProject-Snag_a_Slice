-- This will return all the Pizzerias in a specific city from the database.

SELECT * FROM pizzeria WHERE state = 'Tennessee';

SELECT * FROM pizzeria WHERE state = 'Georgia';

SELECT * FROM pizzeria WHERE state = 'Louisiana';
