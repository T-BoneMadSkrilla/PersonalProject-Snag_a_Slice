-- This will return all the Pizzerias in a specific city from the database.

SELECT * FROM pizzeria WHERE city = 'Nashvile';

SELECT * FROM pizzeria WHERE city = 'Atlanta';

SELECT * FROM pizzeria WHERE city = 'New Orleans';